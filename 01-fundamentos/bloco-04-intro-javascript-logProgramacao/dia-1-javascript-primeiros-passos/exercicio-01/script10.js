// exercicio 10

let custoProd = 15;
let vendaProd = 25;


if (vendaProd >= 0 && vendaProd >=0) {
  let totalCustoProd = custoProd * 1.2
  let lucroProduto = (vendaProd - totalCustoProd) * 1000;
  console.log(lucroProduto);
} else {
  console.log("Error, os valores não podem ser negativos")
}