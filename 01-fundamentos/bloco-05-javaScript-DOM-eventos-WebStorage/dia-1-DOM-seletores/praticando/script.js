
document.querySelector("header").style.backgroundColor = "green";

const cores = document.querySelectorAll(".emergency-tasks");
for(let cores = 0; cores < cores.length; cores += 1){
  cores[cores].style.color = 'blue';
  cores[cores].style.border = '1px solid black';
  cores[cores].style.backgroundColor = 'rebeccaPurple';
  cores[cores].style.textAlign = 'center';
}